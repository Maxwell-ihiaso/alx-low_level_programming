#include "main.h"

/**
 * _abs - checks for case sensitivity
 * @c: Parament
 *
 * Description: Return 1 for lower case letters
 * and 0 for otherwise
 * Return: always returns 0
 */
int _abs(int c)
{
if (c >= 0)
return (c);
else
return (c * -1);
}
