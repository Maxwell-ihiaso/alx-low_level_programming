#include "main.h"

/**
 * _puts - print a string
 * @str: parameter
 * Description: Prints a string ffollowed by a new line
 * Return: Always 0.
 */
void _puts(char *str)
{
	int i;
	
	for (i = 0; *str[i] != '\0'; i++)
		_putchar(s[i]);

}
